version: "0.0"

defaults:
  timeout_ms: 180000
  artifacts_dir: ".artifacts"

suites:
  - id: "api-newman"
    type: "api"
    adapter: "api/newman"
    mode: "execute"
    tags: ["api", "smoke"]

    command: "npx"
    args:
      - "newman"
      - "run"
      - "collection.json"

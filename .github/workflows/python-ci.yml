name: "Example: python ci (Build + unit test)."

on: [push, pull_request]

jobs:
  build:
    uses: BrikByte-Studios/brik-pipe-actions/.github/workflows/build.yml@main
    with:
      stack: "python"
      working_directory: "python-api-example"
      runtime_version: "3.12"
      package_manager: "pip"

      run_lint: true
      run_tests: true

      lint_command: "make lint"
      test_command: "make test"
      build_command: "make build"

      upload_artifacts: true
      artifact_paths: "dist/**,out/**,__pycache__/**"

      # Cache (PIPE-CORE-1.3)
      enable_cache: true
      poetry_cache_mode: "pip-only"

  unit-tests:
    # Keep your advanced enforcement / coverage logic as-is
    uses: BrikByte-Studios/brik-pipe-actions/.github/workflows/unit-test-python.yml@main
    with:
      working_directory: "python-api-example"
      python_version: "3.12"
      dependency_manager: "pip"
      test_command: "make test"
      coverage_mode: "optional"
      enforcement_mode: "warn"

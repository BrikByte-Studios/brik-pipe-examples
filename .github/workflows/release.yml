# =============================================================================
# BrikByteOS — Example Repo Release Workflow
#
# File: brik-pipe-examples/.github/workflows/release.yml
#
# Purpose:
# --------
# Demonstrates downstream consumption of centralized BrikByteOS release tooling:
# - SemVer Tagging (PIPE-CORE-3.1)
# - Changelog Builder (PIPE-CORE-3.2.3)
#
# Governance:
# -----------
# - All release logic is centralized in brik-pipe-actions reusable workflows
# - This repo contains NO custom tagging/changelog logic
# - Prevents policy drift
# =============================================================================

name: Release (BrikByteOS Example)

on:
  workflow_dispatch:
    inputs:
      bump:
        description: "SemVer bump type"
        required: true
        default: patch
        type: choice
        options: [patch, minor, major]
      dry_run:
        description: "Dry run (no tag created)"
        required: true
        default: false
        type: boolean

permissions:
  contents: write

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Debug policy presence
        run: |
          echo "SHA: $(git rev-parse HEAD)"
          ls -la .github || true
          test -f .github/policy.yml && echo "FOUND .github/policy.yml" || echo "MISSING .github/policy.yml"

  release:
    uses: BrikByte-Studios/brik-pipe-actions/.github/workflows/semver-tag.yml@main
    with:
      bump: ${{ inputs.bump }}
      dry_run: ${{ inputs.dry_run }}
      audit_enabled: true
      create_github_release: false
      policy_file: .github/policy.yml

  show_outputs:
    runs-on: ubuntu-latest
    needs: [release]
    steps:
      - run: |
          echo "new_version=${{ needs.release.outputs.new_version }}"
          echo "tag=${{ needs.release.outputs.tag }}"
          echo "sha=${{ needs.release.outputs.sha }}"
          echo "previous_version=${{ needs.release.outputs.previous_version }}"


  changelog:
    name: Build changelog (write)
    needs: [release]
    if: ${{ inputs.dry_run == false }}
    uses: BrikByte-Studios/brik-pipe-actions/.github/workflows/changelog-builder.yml@main
    with:
      mode: write
      # In real usage, pass the actual version produced by semver-tag workflow.
      # If semver-tag exposes an output like `version`, use:
      # version: ${{ needs.release.outputs.version }}
      #
      # For now, if you don't have that output wired yet, you can require manual input
      # or keep preview mode until 3.1 outputs are finalized.
      version: ${{ needs.release.outputs.new_version }}
      output_file: CHANGELOG.md
      enforcement_mode: warn
      include_merges: false
      audit_enabled: true
      commit_changes: true
      bot_name: brikbyte-bot
      bot_email: brikbyte-bot@users.noreply.github.com
      commit_message: "chore(release): update changelog for ${{ needs.release.outputs.new_version }}"

services:
  app:
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: node-ui-example-app
    environment:
      - PORT=3000
    ports:
      - "3000:3000"
    networks:
      - e2e-net

networks:
  e2e-net:
    external: true
